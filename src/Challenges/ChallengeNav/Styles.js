import styled from "styled-components";

export const Nav = styled.nav`
    position: fixed;
    top: 0;
    left: 0;

    display: flex;
    color: blue;

    svg {
        fill: var(--color-main);
    }
`;
