import styled from "styled-components";

export const Nav = styled.nav`
    position: fixed;
    top: 0;
    left: 0;

    display: flex;

    svg {
        fill: var(--color-main);
    }
    z-index: 999;
`;
