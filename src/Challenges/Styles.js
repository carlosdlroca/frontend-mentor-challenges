import styled from "styled-components";

export const ChallengesListPage = styled.main`
    h1 {
        text-align: center;
        font-size: 5rem;
        text-decoration: underline;
        text-decoration-color: var(--color-main);
        text-decoration-style: wavy;
    }
`;
